name: Dev-mael CI

on:
  push:
    branches: [ dev-mael ]
  pull_request:
    branches: [ dev-mael ]

jobs:
  test-and-ff:
    runs-on: ubuntu-latest
    permissions: 
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        id: tests
        run: |
          pytest test/ -v
        continue-on-error: true

      - name: Fast-forward master into dev-mael
        if: steps.tests.outcome == 'success'
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git fetch origin master
          git checkout dev-mael
          git merge --ff-only origin/master || echo "No changes to merge"
          git push origin dev-mael

      - name: Handle test failure
        if: steps.tests.outcome != 'success'
        run: |
          echo "Tests failed → creating failure branch and rolling back dev-mael"

          FAILURE_BRANCH="failure/${GITHUB_RUN_NUMBER}"
          DEV_BRANCH="dev-mael"
          CURRENT_SHA=$(git rev-parse HEAD)
          PREV_SHA=$(git rev-parse HEAD^)

          # Créer branche
          git checkout -b $FAILURE_BRANCH
          git push origin $FAILURE_BRANCH

          # Rollback dev-mael
          git checkout $DEV_BRANCH
          git reset --hard $PREV_SHA
          git push origin $DEV_BRANCH --force